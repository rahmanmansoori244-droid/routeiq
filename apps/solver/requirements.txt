fastapi==0.111.0
uvicorn[standard]==0.30.1
# Solver: PyVRP (Hybrid Genetic Search) — state-of-the-art for CVRP, replaces
# the v1 OR-Tools solver. Still keep ortools as a pinned fallback dependency
# in case we need to A/B test or roll back; it adds ~30 MB to the image but
# zero runtime cost when unused.
pyvrp>=0.13.3
ortools>=9.10
# Module B: Mapbox Directions Matrix HTTP client.
httpx>=0.27,<0.30

# Dev / CI
pytest>=8.0
